name: Deploy HTML
on:
  push:
    branches: [ main ]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout código
        uses: actions/checkout@v3
      - name: Simular despliegue
        run: echo "Desplegando index.html a S3..."
